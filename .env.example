# Copy to .env.public. These URLs are public and included in the installer.
# Leave blank until the relay / donation page is ready.
LIMIT_FEEDBACK_URL=
LIMIT_DONATION_URL=

# For local development ONLY, put this in .env.local:
# LIMIT_FEEDBACK_DEV_URL=http://127.0.0.1:8787/feedback
# Telegram credentials belong ONLY in services/feedback/.env.
