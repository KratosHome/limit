# Встановщики Limit

## Як зібрати встановщики

З кореня проєкту після встановлення залежностей (`npm ci`):

```bash
npm run build:mac # macOS: universal DMG для Intel та Apple Silicon
npm run build:win # Windows: EXE для x64
npm run build:all # послідовно обидва встановщики на підготовленому Mac
```

Команди визначені в `scripts` файлу `package.json`. `build:mac` викликає
`installers:mac`, а `build:win` — `installers:win`; попередні назви також
працюють. Звичайний `npm run build` збирає лише renderer.

Результат у цій папці:

- `Limit-Local-<версія>-mac-universal.dmg` — macOS, Intel та Apple Silicon.
- `Limit-Local-<версія>-win-x64.exe` — Windows x64.

Версія береться з `package.json`. Проміжні файли й ZIP зберігаються у
`release/local/`. Команди не публікують реліз. `npm run installers:collect`
повторно копіює локальні встановщики з цієї папки.

**У локальних збірках production-оновлення вимкнено.** Вихідний код має базову
версію `0.1.0`, а CI призначає релізам окремі номери. Тому локальна збірка
може містити новіший код, ніж опублікована `0.1.6`, хоча число версії менше.
Збірки `Limit-Local` не пропонують замінити ці зміни старішим релізом.
Щоб отримувати наступні оновлення, встановіть файл без `Local` у назві з
[GitHub Releases](https://github.com/KratosHome/limit/releases).

Mac-збірка потребує macOS і Xcode Command Line Tools. Windows-збірку після
чистого `npm ci` запускайте на Windows: залежність `get-windows` встановлює
нативний модуль лише для поточної ОС. Для `build:win` або `build:all` на Mac
потрібно окремо підготувати Windows-модуль. GitHub Actions використовує окремі
Windows і macOS runners.

Збірка не потребує платних сертифікатів. Windows-встановщик не підписаний;
Mac-застосунок має локальний ad-hoc підпис і не нотаризований Apple.

## Як встановити

**Windows:** відкрийте `.exe` і пройдіть встановлення. Система може показати
попередження про невідомого видавця; політики деяких комп’ютерів можуть
забороняти запуск непідписаних програм.

**Mac:** завершіть старий Limit через його меню → «Вийти». Відкрийте `.dmg`,
перетягніть Limit у Applications, підтвердьте заміну та запустіть звідти.
Якщо macOS не дозволяє запуск невідомого розробника, після спроби відкриття
скористайтесь System Settings → Privacy & Security → Open Anyway для Limit,
якщо довіряєте джерелу файлу. Не потрібно вимикати захист для всього Mac.

## Оновлення

Цей розділ стосується встановщиків із GitHub Releases. Локальні встановщики
`Limit-Local` оновлюються повторною збіркою й ручним встановленням.

Після першого push коду з workflow у `main` GitHub Actions збирає й публікує
нову версію. Кожний наступний push у `main` створює наступний реліз лише після
успішних перевірок та збірок обох ОС.

Push не змінює вже встановлений застосунок одразу. Спочатку дочекайтесь
успішного запуску [Release у GitHub Actions](https://github.com/KratosHome/limit/actions/workflows/release.yml)
і появи нової версії у GitHub Releases. Якщо перевірка або збірка хоча б однієї
ОС завершилася помилкою, спільний реліз не публікується. Помилка
`No published versions on GitHub` у `logs/updates.log` означає, що Limit ще
не знайшов жодного опублікованого релізу. Після його появи виберіть перевірку
оновлень у меню Limit біля годинника.

- **Windows:** Limit перевіряє оновлення при запуску та кожні чотири години.
  Завантаження починається після натискання кнопки, а встановлення — через
  «Перезапустити й оновити». Звичайний вихід не встановлює оновлення.
- **Mac:** кнопка «Завантажити» завантажує потрібний DMG у підпапку
  `Limit-Update-…` у «Завантаженнях». Прогрес видно в Dock і меню Limit біля
  годинника. Після перевірки файла натисніть «Відкрити встановщик і завершити
  Limit», перетягніть Limit на Applications із заміною та запустіть його звідти.
  Кнопка «Показати у Finder» дозволяє знайти завантажений файл пізніше.
  Завершення Limit під час завантаження скасовує його; можна почати знову
  після наступного запуску. Заміна застосунку залишається ручною.

У старих версіях кнопка відкриває GitHub. Щоб один раз перейти на версію з
прямим завантаженням, у найновішому релізі розгорніть **Assets** і виберіть
`Limit-<версія>-mac-universal.dmg`. Відкрийте DMG, завершіть Limit через його
меню, замініть застосунок у Applications та запустіть його звідти.

Статистика й налаштування зберігаються окремо від встановленого застосунку.
Нові файли також доступні на [GitHub Releases](https://github.com/KratosHome/limit/releases).

Самі `.exe`/`.dmg` виключені з Git через великий розмір. Ця інструкція та код
збірки входять у репозиторій, а для завантаження людьми файли публікує GitHub
Releases. Службові `.zip`, `.yml` і `.blockmap` у релізі потрібно залишати.
